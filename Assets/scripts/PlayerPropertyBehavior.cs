using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerPropertyBehavior : MonoBehaviour {

    public GameObject OnDeathParticle;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //var pe = Instantiate(OnDeathParticle, gameObject.transform.position, gameObject.transform.rotation);
        //Destroy(OnDeathParticle, 30000);
    }
}
